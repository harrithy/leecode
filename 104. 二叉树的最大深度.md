# 二叉树的最大深度

## 题目描述

给定一棵二叉树，返回它的最大深度。

## 示例

```txt
输入：root = [3,9,20,null,null,15,7]
输出：3
```

## 解题思路

- 最大深度等于左右子树最大深度中的较大值再加 1。
- 空节点深度为 0。
- 这是最经典的树形递归模板。

## 代码实现（JavaScript）

```javascript
var maxDepth = function (root) {
  if (root === null) return 0;
  return Math.max(maxDepth(root.left), maxDepth(root.right)) + 1;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(h)
