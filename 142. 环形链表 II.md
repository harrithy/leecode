# 环形链表 II

## 题目描述

给定一个链表，若存在环，返回环的入口节点；否则返回 null。

## 示例

```txt
输入：head = [3,2,0,-4], pos = 1
输出：返回索引为 1 的链表节点
```

## 解题思路

- 先用快慢指针判断是否有环；若能相遇，说明链表存在环。
- 相遇后让一个指针回到头节点，另一个留在相遇点，两者同步前进。
- 再次相遇的位置就是环入口，这是 Floyd 判圈的经典结论。

## 代码实现（JavaScript）

```javascript
var detectCycle = function (head) {
  let slow = head;
  let fast = head;

  while (fast !== null && fast.next !== null) {
    slow = slow.next;
    fast = fast.next.next;
    if (slow === fast) {
      let ptr = head;
      while (ptr !== slow) {
        ptr = ptr.next;
        slow = slow.next;
      }
      return ptr;
    }
  }

  return null;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
