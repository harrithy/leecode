# N 皇后

## 题目描述

在 n × n 的棋盘上放置 n 个皇后，使任意两个皇后不在同一行、同一列、同一对角线上。

## 示例

```txt
输入：n = 4
输出：[[".Q..","...Q","Q...","..Q."],["..Q.","Q...","...Q",".Q.."]]
```

## 解题思路

- 按行回溯，每一行只放一个皇后。
- 用三个集合分别记录已占用的列、主对角线和副对角线。
- 当前位置合法就放置皇后，递归下一行，回溯时撤销状态。

## 代码实现（JavaScript）

```javascript
var solveNQueens = function (n) {
  const res = [];
  const board = Array.from({ length: n }, () => new Array(n).fill("."));
  const cols = new Set();
  const diag1 = new Set();
  const diag2 = new Set();

  const dfs = (row) => {
    if (row === n) {
      res.push(board.map((line) => line.join("")));
      return;
    }

    for (let col = 0; col < n; col++) {
      const d1 = row - col;
      const d2 = row + col;
      if (cols.has(col) || diag1.has(d1) || diag2.has(d2)) continue;

      board[row][col] = "Q";
      cols.add(col);
      diag1.add(d1);
      diag2.add(d2);

      dfs(row + 1);

      board[row][col] = ".";
      cols.delete(col);
      diag1.delete(d1);
      diag2.delete(d2);
    }
  };

  dfs(0);
  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n!)
- 空间复杂度：O(n)
