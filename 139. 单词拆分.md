# 单词拆分

## 题目描述

给定字符串 s 和字典 wordDict，判断是否可以用字典中的单词拼接出 s。

## 示例

```txt
输入：s = "leetcode", wordDict = ["leet","code"]
输出：true
```

## 解题思路

- 设 dp[i] 表示前 i 个字符能否被成功拆分。
- 若某个位置 j 满足 dp[j] 为真，且 s[j..i-1] 在字典中，则 dp[i] 为真。
- 用哈希集合保存字典单词，能把查找降到 O(1)。

## 代码实现（JavaScript）

```javascript
var wordBreak = function (s, wordDict) {
  const set = new Set(wordDict);
  const dp = new Array(s.length + 1).fill(false);
  dp[0] = true;

  for (let i = 1; i <= s.length; i++) {
    for (let j = 0; j < i; j++) {
      if (dp[j] && set.has(s.slice(j, i))) {
        dp[i] = true;
        break;
      }
    }
  }

  return dp[s.length];
};
```

## 复杂度分析

- 时间复杂度：O(n^2)
- 空间复杂度：O(n + m)
