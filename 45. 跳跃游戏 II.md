# 跳跃游戏 II

## 题目描述

给定一个非负整数数组，每个位置表示最远可跳长度，求到达末尾的最少跳跃次数。

## 示例

```txt
输入：nums = [2,3,1,1,4]
输出：2
```

## 解题思路

- 贪心地按层推进，currentEnd 表示当前步数能覆盖到的最远边界。
- 遍历当前层的所有位置时，持续更新下一层最远可达位置 farthest。
- 当遍历到 currentEnd，就必须再跳一步，并把边界更新为 farthest。

## 代码实现（JavaScript）

```javascript
var jump = function (nums) {
  let steps = 0;
  let currentEnd = 0;
  let farthest = 0;

  for (let i = 0; i < nums.length - 1; i++) {
    farthest = Math.max(farthest, i + nums[i]);
    if (i === currentEnd) {
      steps++;
      currentEnd = farthest;
    }
  }

  return steps;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
