# 寻找重复数

## 题目描述

给定包含 n + 1 个整数的数组 nums，数字范围在 1 到 n 之间，找出唯一重复的数字。

## 示例

```txt
输入：nums = [1,3,4,2,2]
输出：2
```

## 解题思路

- 把数组看成链表：下标 i 指向 nums[i]。
- 因为存在重复数，所以一定会形成环，入口就是重复的数字。
- 用 Floyd 判圈，先找相遇点，再找环入口。

## 代码实现（JavaScript）

```javascript
var findDuplicate = function (nums) {
  let slow = nums[0];
  let fast = nums[0];

  do {
    slow = nums[slow];
    fast = nums[nums[fast]];
  } while (slow !== fast);

  let ptr = nums[0];
  while (ptr !== slow) {
    ptr = nums[ptr];
    slow = nums[slow];
  }

  return ptr;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
