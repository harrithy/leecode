# 二叉树中的最大路径和

## 题目描述

给定一棵二叉树，求任意路径的最大路径和，路径不要求经过根节点。

## 示例

```txt
输入：root = [1,2,3]
输出：6
```

## 解题思路

- 对每个节点，向父节点返回的只能是一条向下延伸的路径，因此返回值是 node.val + max(0, leftGain, rightGain)。
- 但更新全局答案时，可以把左右两边都接上，形成穿过当前节点的完整路径。
- 树形 DP 的关键是区分“返回父节点的值”和“更新全局最优值”。

## 代码实现（JavaScript）

```javascript
var maxPathSum = function (root) {
  let ans = -Infinity;

  const dfs = (node) => {
    if (node === null) return 0;

    const leftGain = Math.max(dfs(node.left), 0);
    const rightGain = Math.max(dfs(node.right), 0);

    ans = Math.max(ans, node.val + leftGain + rightGain);
    return node.val + Math.max(leftGain, rightGain);
  };

  dfs(root);
  return ans;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(h)
