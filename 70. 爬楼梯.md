# 爬楼梯

## 题目描述

每次可以爬 1 阶或 2 阶，给定 n 阶楼梯，求共有多少种不同爬法。

## 示例

```txt
输入：n = 3
输出：3
```

## 解题思路

- 到第 i 阶的方法数等于到第 i - 1 阶和第 i - 2 阶的方法数之和。
- 这是斐波那契模型，只需要保留前两个状态即可。
- 从小到大迭代到 n，返回最终结果。

## 代码实现（JavaScript）

```javascript
var climbStairs = function (n) {
  if (n <= 2) return n;

  let a = 1;
  let b = 2;

  for (let i = 3; i <= n; i++) {
    const c = a + b;
    a = b;
    b = c;
  }

  return b;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
