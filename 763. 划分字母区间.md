# 划分字母区间

## 题目描述

把字符串划分成尽可能多的片段，使每个字母最多出现在一个片段中。

## 示例

```txt
输入：s = "ababcbacadefegdehijhklij"
输出：[9,7,8]
```

## 解题思路

- 先记录每个字符最后一次出现的位置。
- 扫描字符串时不断更新当前片段的最远右边界 end。
- 当 i === end，说明当前片段可以独立切分。

## 代码实现（JavaScript）

```javascript
var partitionLabels = function (s) {
  const last = new Map();
  for (let i = 0; i < s.length; i++) {
    last.set(s[i], i);
  }

  const res = [];
  let start = 0;
  let end = 0;

  for (let i = 0; i < s.length; i++) {
    end = Math.max(end, last.get(s[i]));
    if (i === end) {
      res.push(end - start + 1);
      start = i + 1;
    }
  }

  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
