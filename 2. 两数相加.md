# 两数相加

## 题目描述

给你两个非空链表，分别表示两个非负整数，数字按逆序存储。请把它们相加并返回一个新的链表。

## 示例

```txt
输入：l1 = [2,4,3], l2 = [5,6,4]
输出：[7,0,8]
```

## 解题思路

- 同时遍历两个链表，并维护当前位的进位 carry。
- 每次把两个节点值与 carry 相加，新节点值为 sum % 10，新的进位为 Math.floor(sum / 10)。
- 为了方便拼接结果，使用虚拟头节点 dummy。

## 代码实现（JavaScript）

```javascript
var addTwoNumbers = function (l1, l2) {
  const dummy = new ListNode(0);
  let cur = dummy;
  let carry = 0;

  while (l1 !== null || l2 !== null || carry !== 0) {
    const x = l1 ? l1.val : 0;
    const y = l2 ? l2.val : 0;
    const sum = x + y + carry;

    carry = Math.floor(sum / 10);
    cur.next = new ListNode(sum % 10);
    cur = cur.next;

    if (l1) l1 = l1.next;
    if (l2) l2 = l2.next;
  }

  return dummy.next;
};
```

## 复杂度分析

- 时间复杂度：O(max(m, n))
- 空间复杂度：O(max(m, n))
