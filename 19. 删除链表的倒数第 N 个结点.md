# 删除链表的倒数第 N 个结点

## 题目描述

给你一个链表 head，删除倒数第 n 个结点，并返回链表头节点。

## 示例

```txt
输入：head = [1,2,3,4,5], n = 2
输出：[1,2,3,5]
```

## 解题思路

- 使用虚拟头节点，避免删除头节点时的分类讨论。
- fast 先走 n 步，然后 slow 和 fast 一起走，直到 fast 到末尾。
- 此时 slow 的下一个节点就是要删除的节点。

## 代码实现（JavaScript）

```javascript
var removeNthFromEnd = function (head, n) {
  const dummy = new ListNode(0, head);
  let fast = dummy;
  let slow = dummy;

  for (let i = 0; i < n; i++) {
    fast = fast.next;
  }

  while (fast.next !== null) {
    fast = fast.next;
    slow = slow.next;
  }

  slow.next = slow.next.next;
  return dummy.next;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
