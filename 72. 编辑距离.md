# 编辑距离

## 题目描述

给定两个单词 word1 和 word2，求把 word1 转换成 word2 所需的最少操作数。

## 示例

```txt
输入：word1 = "horse", word2 = "ros"
输出：3
```

## 解题思路

- 设 dp[i][j] 表示 word1 前 i 个字符变成 word2 前 j 个字符的最小操作数。
- 若末尾字符相同，继承 dp[i - 1][j - 1]；否则从插入、删除、替换三种操作里取最小值。
- 边界是空串和非空串之间的转换次数。

## 代码实现（JavaScript）

```javascript
var minDistance = function (word1, word2) {
  const m = word1.length;
  const n = word2.length;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));

  for (let i = 0; i <= m; i++) dp[i][0] = i;
  for (let j = 0; j <= n; j++) dp[0][j] = j;

  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (word1[i - 1] === word2[j - 1]) {
        dp[i][j] = dp[i - 1][j - 1];
      } else {
        dp[i][j] =
          Math.min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1;
      }
    }
  }

  return dp[m][n];
};
```

## 复杂度分析

- 时间复杂度：O(m * n)
- 空间复杂度：O(m * n)
