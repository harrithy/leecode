# 最长有效括号

## 题目描述

给定只包含 '(' 和 ')' 的字符串，求最长有效括号子串长度。

## 示例

```txt
输入：s = ")()())"
输出：4
```

## 解题思路

- 用栈保存尚未匹配的位置下标。
- 栈底先放一个哨兵 -1，方便计算长度。
- 遇到右括号时若匹配失败，就把当前位置当作新的起点；若匹配成功，用当前下标减去栈顶更新答案。

## 代码实现（JavaScript）

```javascript
var longestValidParentheses = function (s) {
  const stack = [-1];
  let ans = 0;

  for (let i = 0; i < s.length; i++) {
    if (s[i] === "(") {
      stack.push(i);
    } else {
      stack.pop();
      if (stack.length === 0) {
        stack.push(i);
      } else {
        ans = Math.max(ans, i - stack[stack.length - 1]);
      }
    }
  }

  return ans;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
