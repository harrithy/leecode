# 每日温度

## 题目描述

给定每天的气温，返回距离下一次更高温度还需要等待的天数。

## 示例

```txt
输入：temperatures = [73,74,75,71,69,72,76,73]
输出：[1,1,4,2,1,1,0,0]
```

## 解题思路

- 用单调递减栈保存还没找到更高温度的下标。
- 当今天温度高于栈顶那天，就能确定栈顶答案为下标差。
- 每个下标只会进栈出栈一次。

## 代码实现（JavaScript）

```javascript
var dailyTemperatures = function (temperatures) {
  const res = new Array(temperatures.length).fill(0);
  const stack = [];

  for (let i = 0; i < temperatures.length; i++) {
    while (
      stack.length > 0 &&
      temperatures[i] > temperatures[stack[stack.length - 1]]
    ) {
      const index = stack.pop();
      res[index] = i - index;
    }
    stack.push(i);
  }

  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
