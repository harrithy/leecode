# 乘积最大子数组

## 题目描述

给定一个整数数组 nums，找出乘积最大的连续子数组，并返回该乘积。

## 示例

```txt
输入：nums = [2,3,-2,4]
输出：6
```

## 解题思路

- 因为负数会让最大值和最小值互换，所以需要同时维护当前位置结尾的最大乘积和最小乘积。
- 遍历到 nums[i] 时，新的最大值和最小值都可能来自 nums[i] 本身、maxProd * nums[i]、minProd * nums[i]。
- 持续更新全局答案即可。

## 代码实现（JavaScript）

```javascript
var maxProduct = function (nums) {
  let maxProd = nums[0];
  let minProd = nums[0];
  let ans = nums[0];

  for (let i = 1; i < nums.length; i++) {
    const num = nums[i];
    const candidates = [num, maxProd * num, minProd * num];
    maxProd = Math.max(...candidates);
    minProd = Math.min(...candidates);
    ans = Math.max(ans, maxProd);
  }

  return ans;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
