# 子集

## 题目描述

给定一个互不相同的整数数组 nums，返回它的所有子集。

## 示例

```txt
输入：nums = [1,2,3]
输出：[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]
```

## 解题思路

- 每个元素都只有选或不选两种状态，本质上是回溯枚举。
- 先把当前 path 放进答案，再从当前位置开始继续尝试加入后续元素。
- 这样自然能覆盖所有大小不同的组合。

## 代码实现（JavaScript）

```javascript
var subsets = function (nums) {
  const res = [];
  const path = [];

  const dfs = (start) => {
    res.push([...path]);
    for (let i = start; i < nums.length; i++) {
      path.push(nums[i]);
      dfs(i + 1);
      path.pop();
    }
  };

  dfs(0);
  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n * 2^n)
- 空间复杂度：O(n)
