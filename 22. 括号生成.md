# 括号生成

## 题目描述

给定 n 对括号，请生成所有合法的括号组合。

## 示例

```txt
输入：n = 3
输出：["((()))","(()())","(())()","()(())","()()()"]
```

## 解题思路

- 回溯时维护左右括号已使用数量。
- 左括号数量还没到 n 时可以继续放左括号。
- 右括号数量小于左括号数量时，才允许放右括号，保证过程始终合法。

## 代码实现（JavaScript）

```javascript
var generateParenthesis = function (n) {
  const res = [];
  const path = [];

  const dfs = (left, right) => {
    if (path.length === 2 * n) {
      res.push(path.join(""));
      return;
    }

    if (left < n) {
      path.push("(");
      dfs(left + 1, right);
      path.pop();
    }

    if (right < left) {
      path.push(")");
      dfs(left, right + 1);
      path.pop();
    }
  };

  dfs(0, 0);
  return res;
};
```

## 复杂度分析

- 时间复杂度：O(Cn)
- 空间复杂度：O(n)
