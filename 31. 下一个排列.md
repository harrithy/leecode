# 下一个排列

## 题目描述

给定一个整数数组 nums，请将其调整为字典序中的下一个更大排列；若不存在，则改成最小排列。

## 示例

```txt
输入：nums = [1,2,3]
输出：[1,3,2]
```

## 解题思路

- 从右向左找第一个升序位置 i，使 nums[i] < nums[i + 1]。
- 再从右向左找第一个大于 nums[i] 的数，与之交换。
- 最后把 i 后面的部分反转，让后缀变成最小升序。

## 代码实现（JavaScript）

```javascript
var nextPermutation = function (nums) {
  let i = nums.length - 2;
  while (i >= 0 && nums[i] >= nums[i + 1]) i--;

  if (i >= 0) {
    let j = nums.length - 1;
    while (nums[j] <= nums[i]) j--;
    [nums[i], nums[j]] = [nums[j], nums[i]];
  }

  let left = i + 1;
  let right = nums.length - 1;
  while (left < right) {
    [nums[left], nums[right]] = [nums[right], nums[left]];
    left++;
    right--;
  }
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
