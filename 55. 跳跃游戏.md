# 跳跃游戏

## 题目描述

给定一个非负整数数组 nums，判断是否能够从起点跳到最后一个位置。

## 示例

```txt
输入：nums = [2,3,1,1,4]
输出：true
```

## 解题思路

- 维护当前能到达的最远位置 maxReach。
- 遍历到位置 i 时，如果 i 已经超过 maxReach，说明这里根本到不了。
- 否则更新 maxReach = Math.max(maxReach, i + nums[i])，只要最终能覆盖到最后一个下标即可。

## 代码实现（JavaScript）

```javascript
var canJump = function (nums) {
  let maxReach = 0;

  for (let i = 0; i < nums.length; i++) {
    if (i > maxReach) return false;
    maxReach = Math.max(maxReach, i + nums[i]);
  }

  return true;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
