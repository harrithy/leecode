# 不同路径

## 题目描述

机器人从左上角走到右下角，只能向右或向下移动，求一共有多少种不同路径。

## 示例

```txt
输入：m = 3, n = 7
输出：28
```

## 解题思路

- 设 dp[j] 表示当前行走到第 j 列的路径数。
- 当前位置只能从上方或左侧到达，所以转移为 dp[j] += dp[j - 1]。
- 用一维数组即可完成空间优化。

## 代码实现（JavaScript）

```javascript
var uniquePaths = function (m, n) {
  const dp = new Array(n).fill(1);

  for (let i = 1; i < m; i++) {
    for (let j = 1; j < n; j++) {
      dp[j] += dp[j - 1];
    }
  }

  return dp[n - 1];
};
```

## 复杂度分析

- 时间复杂度：O(m * n)
- 空间复杂度：O(n)
