# 搜索旋转排序数组

## 题目描述

给定一个经过旋转的升序数组和目标值 target，要求在 O(log n) 内找到目标下标。

## 示例

```txt
输入：nums = [4,5,6,7,0,1,2], target = 0
输出：4
```

## 解题思路

- 虽然数组被旋转，但任意时刻总有一半区间是有序的。
- 先判断 mid 落在哪个有序区间，再看 target 是否位于该区间中。
- 根据判断结果收缩左右边界。

## 代码实现（JavaScript）

```javascript
var search = function (nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] === target) return mid;

    if (nums[left] <= nums[mid]) {
      if (nums[left] <= target && target < nums[mid]) {
        right = mid - 1;
      } else {
        left = mid + 1;
      }
    } else {
      if (nums[mid] < target && target <= nums[right]) {
        left = mid + 1;
      } else {
        right = mid - 1;
      }
    }
  }

  return -1;
};
```

## 复杂度分析

- 时间复杂度：O(log n)
- 空间复杂度：O(1)
