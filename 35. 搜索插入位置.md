# 搜索插入位置

## 题目描述

给定一个有序数组和目标值，如果目标存在返回下标，否则返回应该插入的位置。

## 示例

```txt
输入：nums = [1,3,5,6], target = 5
输出：2
```

## 解题思路

- 这是标准 lower_bound 模型。
- 二分结束后 left 指向第一个大于等于 target 的位置。
- 如果 target 比所有元素都大，left 自然会落在数组末尾。

## 代码实现（JavaScript）

```javascript
var searchInsert = function (nums, target) {
  let left = 0;
  let right = nums.length;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] >= target) {
      right = mid;
    } else {
      left = mid + 1;
    }
  }

  return left;
};
```

## 复杂度分析

- 时间复杂度：O(log n)
- 空间复杂度：O(1)
