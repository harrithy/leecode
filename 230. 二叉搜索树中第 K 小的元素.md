# 二叉搜索树中第 K 小的元素

## 题目描述

给定一棵二叉搜索树，返回其中第 k 小的元素。

## 示例

```txt
输入：root = [3,1,4,null,2], k = 1
输出：1
```

## 解题思路

- BST 的中序遍历结果是升序序列。
- 因此按中序遍历第 k 个访问到的节点就是答案。
- 迭代中序可以做到边遍历边计数。

## 代码实现（JavaScript）

```javascript
var kthSmallest = function (root, k) {
  const stack = [];
  let cur = root;

  while (cur !== null || stack.length > 0) {
    while (cur !== null) {
      stack.push(cur);
      cur = cur.left;
    }

    cur = stack.pop();
    k--;
    if (k === 0) return cur.val;
    cur = cur.right;
  }

  return -1;
};
```

## 复杂度分析

- 时间复杂度：O(h + k)
- 空间复杂度：O(h)
