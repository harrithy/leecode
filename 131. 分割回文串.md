# 分割回文串

## 题目描述

给定字符串 s，把它分割成若干子串，要求每个子串都是回文串，返回所有可能方案。

## 示例

```txt
输入：s = "aab"
输出：[["a","a","b"],["aa","b"]]
```

## 解题思路

- 回溯枚举每一刀切在哪个位置。
- 若 s[start..end] 是回文串，就把它加入路径并继续处理后缀。
- 回文判断可以直接双指针检查，逻辑直观。

## 代码实现（JavaScript）

```javascript
var partition = function (s) {
  const res = [];
  const path = [];

  const isPalindrome = (left, right) => {
    while (left < right) {
      if (s[left] !== s[right]) return false;
      left++;
      right--;
    }
    return true;
  };

  const dfs = (start) => {
    if (start === s.length) {
      res.push([...path]);
      return;
    }

    for (let end = start; end < s.length; end++) {
      if (!isPalindrome(start, end)) continue;
      path.push(s.slice(start, end + 1));
      dfs(end + 1);
      path.pop();
    }
  };

  dfs(0);
  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n * 2^n)
- 空间复杂度：O(n)
