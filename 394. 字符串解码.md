# 字符串解码

## 题目描述

给定编码字符串，按 k[encoded_string] 的规则解码并返回结果。

## 示例

```txt
输入：s = "3[a2[c]]"
输出："accaccacc"
```

## 解题思路

- 遇到数字时累计倍数，遇到左括号时把当前字符串和倍数压栈。
- 遇到右括号时弹出一层，做 prev + cur.repeat(times) 拼接。
- 普通字母直接追加到当前字符串。

## 代码实现（JavaScript）

```javascript
var decodeString = function (s) {
  const countStack = [];
  const strStack = [];
  let current = "";
  let num = 0;

  for (const ch of s) {
    if (ch >= "0" && ch <= "9") {
      num = num * 10 + Number(ch);
    } else if (ch === "[") {
      countStack.push(num);
      strStack.push(current);
      num = 0;
      current = "";
    } else if (ch === "]") {
      const times = countStack.pop();
      const prev = strStack.pop();
      current = prev + current.repeat(times);
    } else {
      current += ch;
    }
  }

  return current;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
