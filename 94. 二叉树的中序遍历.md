# 二叉树的中序遍历

## 题目描述

给定一棵二叉树，按中序遍历顺序返回节点值。

## 示例

```txt
输入：root = [1,null,2,3]
输出：[1,3,2]
```

## 解题思路

- 中序遍历顺序是左 -> 根 -> 右。
- 迭代写法用栈一路向左压入节点。
- 弹出节点后访问它，再转去处理右子树。

## 代码实现（JavaScript）

```javascript
var inorderTraversal = function (root) {
  const res = [];
  const stack = [];
  let cur = root;

  while (cur !== null || stack.length > 0) {
    while (cur !== null) {
      stack.push(cur);
      cur = cur.left;
    }

    cur = stack.pop();
    res.push(cur.val);
    cur = cur.right;
  }

  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
