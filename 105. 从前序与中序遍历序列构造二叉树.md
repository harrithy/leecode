# 从前序与中序遍历序列构造二叉树

## 题目描述

给定一棵树的前序遍历和中序遍历结果，请重建这棵二叉树。

## 示例

```txt
输入：preorder = [3,9,20,15,7], inorder = [9,3,15,20,7]
输出：[3,9,20,null,null,15,7]
```

## 解题思路

- 前序遍历的第一个元素一定是当前子树根节点。
- 在中序遍历中找到根的位置，就能确定左右子树的节点数量。
- 配合哈希表定位中序下标，用递归构建左右子树。

## 代码实现（JavaScript）

```javascript
var buildTree = function (preorder, inorder) {
  const indexMap = new Map();
  for (let i = 0; i < inorder.length; i++) {
    indexMap.set(inorder[i], i);
  }

  const dfs = (preLeft, preRight, inLeft, inRight) => {
    if (preLeft > preRight) return null;

    const rootVal = preorder[preLeft];
    const rootIndex = indexMap.get(rootVal);
    const leftSize = rootIndex - inLeft;
    const root = new TreeNode(rootVal);

    root.left = dfs(preLeft + 1, preLeft + leftSize, inLeft, rootIndex - 1);
    root.right = dfs(preLeft + leftSize + 1, preRight, rootIndex + 1, inRight);
    return root;
  };

  return dfs(0, preorder.length - 1, 0, inorder.length - 1);
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
