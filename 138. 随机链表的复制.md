# 随机链表的复制

## 题目描述

给定一个带 random 指针的链表，返回它的深拷贝。

## 示例

```txt
输入：head = [[7,null],[13,0],[11,4],[10,2],[1,0]]
输出：[[7,null],[13,0],[11,4],[10,2],[1,0]]
```

## 解题思路

- 第一遍遍历时，建立原节点到新节点的映射。
- 第二遍遍历时，根据映射补上 next 和 random 指针。
- 哈希表方法最稳妥，也最容易写对。

## 代码实现（JavaScript）

```javascript
var copyRandomList = function (head) {
  if (head === null) return null;

  const map = new Map();
  let cur = head;

  while (cur !== null) {
    map.set(cur, new Node(cur.val));
    cur = cur.next;
  }

  cur = head;
  while (cur !== null) {
    const copy = map.get(cur);
    copy.next = cur.next ? map.get(cur.next) : null;
    copy.random = cur.random ? map.get(cur.random) : null;
    cur = cur.next;
  }

  return map.get(head);
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
