# 二叉树的层序遍历

## 题目描述

按层从上到下遍历二叉树，返回每一层的节点值。

## 示例

```txt
输入：root = [3,9,20,null,null,15,7]
输出：[[3],[9,20],[15,7]]
```

## 解题思路

- 层序遍历天然适合用队列。
- 每轮先记录当前队列长度 size，这一轮只处理同一层的 size 个节点。
- 处理完后把该层结果放进答案，再继续下一层。

## 代码实现（JavaScript）

```javascript
var levelOrder = function (root) {
  if (root === null) return [];

  const res = [];
  const queue = [root];

  while (queue.length > 0) {
    const size = queue.length;
    const level = [];

    for (let i = 0; i < size; i++) {
      const node = queue.shift();
      level.push(node.val);
      if (node.left !== null) queue.push(node.left);
      if (node.right !== null) queue.push(node.right);
    }

    res.push(level);
  }

  return res;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
