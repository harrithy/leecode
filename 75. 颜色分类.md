# 颜色分类

## 题目描述

给定只包含 0、1、2 的数组，请原地排序，使同色元素相邻，顺序为红白蓝。

## 示例

```txt
输入：nums = [2,0,2,1,1,0]
输出：[0,0,1,1,2,2]
```

## 解题思路

- 使用荷兰国旗问题的三指针做法。
- left 左边全是 0，right 右边全是 2，i 在中间扫描。
- 遇到 0 交换到左边，遇到 2 交换到右边，遇到 1 直接跳过。

## 代码实现（JavaScript）

```javascript
var sortColors = function (nums) {
  let left = 0;
  let right = nums.length - 1;
  let i = 0;

  while (i <= right) {
    if (nums[i] === 0) {
      [nums[i], nums[left]] = [nums[left], nums[i]];
      i++;
      left++;
    } else if (nums[i] === 2) {
      [nums[i], nums[right]] = [nums[right], nums[i]];
      right--;
    } else {
      i++;
    }
  }
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
