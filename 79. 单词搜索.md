# 单词搜索

## 题目描述

给定字符网格 board 和字符串 word，判断 word 是否可以由相邻格子顺序组成。

## 示例

```txt
输入：board = [["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]], word = "ABCCED"
输出：true
```

## 解题思路

- 从每个起点出发做 DFS，尝试匹配 word 的当前位置。
- 进入某个格子后先标记已访问，递归探索上下左右，再回溯恢复。
- 只要有一条路径能完整匹配整个单词，就返回 true。

## 代码实现（JavaScript）

```javascript
var exist = function (board, word) {
  const m = board.length;
  const n = board[0].length;

  const dfs = (i, j, index) => {
    if (index === word.length) return true;
    if (i < 0 || i >= m || j < 0 || j >= n || board[i][j] !== word[index]) {
      return false;
    }

    const ch = board[i][j];
    board[i][j] = "#";
    const found =
      dfs(i + 1, j, index + 1) ||
      dfs(i - 1, j, index + 1) ||
      dfs(i, j + 1, index + 1) ||
      dfs(i, j - 1, index + 1);
    board[i][j] = ch;
    return found;
  };

  for (let i = 0; i < m; i++) {
    for (let j = 0; j < n; j++) {
      if (dfs(i, j, 0)) return true;
    }
  }

  return false;
};
```

## 复杂度分析

- 时间复杂度：O(m * n * 4^L)
- 空间复杂度：O(L)
