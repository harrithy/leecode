# 路径总和 III

## 题目描述

给定一棵二叉树和目标和 targetSum，统计路径和等于 targetSum 的路径数量。

## 示例

```txt
输入：root = [10,5,-3,3,2,null,11,3,-2,null,1], targetSum = 8
输出：3
```

## 解题思路

- 用前缀和统计从根到当前节点路径上的累计和。
- 若当前前缀和为 curr，之前出现过 curr - targetSum，就说明这些位置到当前节点构成合法路径。
- DFS 进入子树前后分别增加和撤销当前前缀和计数。

## 代码实现（JavaScript）

```javascript
var pathSum = function (root, targetSum) {
  const prefix = new Map();
  prefix.set(0, 1);

  const dfs = (node, current) => {
    if (node === null) return 0;

    current += node.val;
    let count = prefix.get(current - targetSum) || 0;

    prefix.set(current, (prefix.get(current) || 0) + 1);
    count += dfs(node.left, current);
    count += dfs(node.right, current);
    prefix.set(current, prefix.get(current) - 1);

    return count;
  };

  return dfs(root, 0);
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
