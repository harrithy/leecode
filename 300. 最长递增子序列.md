# 最长递增子序列

## 题目描述

给定一个整数数组 nums，求最长严格递增子序列的长度。

## 示例

```txt
输入：nums = [10,9,2,5,3,7,101,18]
输出：4
```

## 解题思路

- tails[i] 记录长度为 i + 1 的递增子序列中最小可能的结尾值。
- 遍历每个数 num，在 tails 中二分找到第一个大于等于它的位置并替换。
- tails 的长度就是答案。

## 代码实现（JavaScript）

```javascript
var lengthOfLIS = function (nums) {
  const tails = [];

  for (const num of nums) {
    let left = 0;
    let right = tails.length;
    while (left < right) {
      const mid = Math.floor((left + right) / 2);
      if (tails[mid] >= num) {
        right = mid;
      } else {
        left = mid + 1;
      }
    }
    tails[left] = num;
  }

  return tails.length;
};
```

## 复杂度分析

- 时间复杂度：O(n log n)
- 空间复杂度：O(n)
