# 打家劫舍

## 题目描述

每间房屋都有一定金额，相邻房屋不能同时偷，求能偷到的最大金额。

## 示例

```txt
输入：nums = [2,7,9,3,1]
输出：12
```

## 解题思路

- 到第 i 间房时只有两种选择：偷它或不偷它。
- 状态转移为 dp[i] = max(dp[i - 1], dp[i - 2] + nums[i])。
- 只依赖前两个状态，可以压缩成常数空间。

## 代码实现（JavaScript）

```javascript
var rob = function (nums) {
  let prev2 = 0;
  let prev1 = 0;

  for (const num of nums) {
    const cur = Math.max(prev1, prev2 + num);
    prev2 = prev1;
    prev1 = cur;
  }

  return prev1;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
