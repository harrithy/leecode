# 分割等和子集

## 题目描述

判断一个正整数数组能否分成两个和相等的子集。

## 示例

```txt
输入：nums = [1,5,11,5]
输出：true
```

## 解题思路

- 若总和为奇数，必然无法平分。
- 问题转化为：能否从数组中选出若干数，使其和恰好等于 total / 2。
- 这是 0/1 背包，使用一维布尔 dp 倒序更新。

## 代码实现（JavaScript）

```javascript
var canPartition = function (nums) {
  const sum = nums.reduce((acc, cur) => acc + cur, 0);
  if (sum % 2 !== 0) return false;

  const target = sum / 2;
  const dp = new Array(target + 1).fill(false);
  dp[0] = true;

  for (const num of nums) {
    for (let j = target; j >= num; j--) {
      dp[j] = dp[j] || dp[j - num];
    }
  }

  return dp[target];
};
```

## 复杂度分析

- 时间复杂度：O(n * target)
- 空间复杂度：O(target)
