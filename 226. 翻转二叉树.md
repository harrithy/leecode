# 翻转二叉树

## 题目描述

把一棵二叉树的左右子树全部交换，返回翻转后的树。

## 示例

```txt
输入：root = [4,2,7,1,3,6,9]
输出：[4,7,2,9,6,3,1]
```

## 解题思路

- 每个节点只做一件事：交换 left 和 right。
- 然后递归处理交换后的左右子树。
- 这是经典的树形递归模板题。

## 代码实现（JavaScript）

```javascript
var invertTree = function (root) {
  if (root === null) return null;
  [root.left, root.right] = [root.right, root.left];
  invertTree(root.left);
  invertTree(root.right);
  return root;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(h)
