# 二叉树的直径

## 题目描述

给定一棵二叉树，求任意两个节点之间最长路径的边数。

## 示例

```txt
输入：root = [1,2,3,4,5]
输出：3
```

## 解题思路

- 对于每个节点，经过它的路径长度等于左子树深度 + 右子树深度。
- 递归函数返回以当前节点为根的最大深度。
- 遍历过程中不断更新全局最大直径。

## 代码实现（JavaScript）

```javascript
var diameterOfBinaryTree = function (root) {
  let ans = 0;

  const depth = (node) => {
    if (node === null) return 0;
    const left = depth(node.left);
    const right = depth(node.right);
    ans = Math.max(ans, left + right);
    return Math.max(left, right) + 1;
  };

  depth(root);
  return ans;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(h)
