# 电话号码的字母组合

## 题目描述

给定仅包含数字 2-9 的字符串 digits，返回它能表示的所有字母组合。

## 示例

```txt
输入：digits = "23"
输出：["ad","ae","af","bd","be","bf","cd","ce","cf"]
```

## 解题思路

- 这是标准回溯题，每个数字都有一组可选字母。
- 从左到右逐位选择，把当前路径加入 path，走到底时收集答案。
- 空字符串没有组合，直接返回空数组。

## 代码实现（JavaScript）

```javascript
var letterCombinations = function (digits) {
  if (digits.length === 0) return [];

  const map = {
    2: "abc",
    3: "def",
    4: "ghi",
    5: "jkl",
    6: "mno",
    7: "pqrs",
    8: "tuv",
    9: "wxyz",
  };
  const res = [];
  const path = [];

  const dfs = (index) => {
    if (index === digits.length) {
      res.push(path.join(""));
      return;
    }

    for (const ch of map[digits[index]]) {
      path.push(ch);
      dfs(index + 1);
      path.pop();
    }
  };

  dfs(0);
  return res;
};
```

## 复杂度分析

- 时间复杂度：O(4^n * n)
- 空间复杂度：O(n)
