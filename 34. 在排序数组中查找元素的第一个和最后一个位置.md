# 在排序数组中查找元素的第一个和最后一个位置

## 题目描述

给定一个有序数组 nums 和目标值 target，找出 target 的起始和结束位置。

## 示例

```txt
输入：nums = [5,7,7,8,8,10], target = 8
输出：[3,4]
```

## 解题思路

- 核心是二分两次：一次找左边界，一次找右边界。
- 左边界二分时，满足 nums[mid] >= target 就继续向左收缩。
- 右边界可以转化成查找 target + 1 的左边界，再减一。

## 代码实现（JavaScript）

```javascript
var searchRange = function (nums, target) {
  const lowerBound = (value) => {
    let left = 0;
    let right = nums.length;
    while (left < right) {
      const mid = Math.floor((left + right) / 2);
      if (nums[mid] >= value) {
        right = mid;
      } else {
        left = mid + 1;
      }
    }
    return left;
  };

  const start = lowerBound(target);
  if (start === nums.length || nums[start] !== target) return [-1, -1];
  const end = lowerBound(target + 1) - 1;
  return [start, end];
};
```

## 复杂度分析

- 时间复杂度：O(log n)
- 空间复杂度：O(1)
