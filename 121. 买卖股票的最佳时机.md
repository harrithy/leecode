# 买卖股票的最佳时机

## 题目描述

给定一支股票每天的价格，只允许买卖一次，求最大利润。

## 示例

```txt
输入：prices = [7,1,5,3,6,4]
输出：5
```

## 解题思路

- 遍历时维护到当前位置为止的最低买入价格 minPrice。
- 当前位置卖出能得到的利润是 prices[i] - minPrice。
- 持续更新最大利润即可。

## 代码实现（JavaScript）

```javascript
var maxProfit = function (prices) {
  let minPrice = Infinity;
  let ans = 0;

  for (const price of prices) {
    minPrice = Math.min(minPrice, price);
    ans = Math.max(ans, price - minPrice);
  }

  return ans;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
