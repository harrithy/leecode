# 二叉树展开为链表

## 题目描述

将二叉树原地展开为右指针串起来的链表，顺序与前序遍历一致。

## 示例

```txt
输入：root = [1,2,5,3,4,null,6]
输出：[1,null,2,null,3,null,4,null,5,null,6]
```

## 解题思路

- 逆前序遍历顺序是右 -> 左 -> 根，用一个 prev 记录已经处理好的链表头。
- 处理当前节点时，把它的 right 指向 prev，left 置空。
- 这样递归回溯时自然会拼出前序顺序的链表。

## 代码实现（JavaScript）

```javascript
var flatten = function (root) {
  let prev = null;

  const dfs = (node) => {
    if (node === null) return;
    dfs(node.right);
    dfs(node.left);
    node.right = prev;
    node.left = null;
    prev = node;
  };

  dfs(root);
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(h)
