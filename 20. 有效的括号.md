# 有效的括号

## 题目描述

给定一个只包含括号字符的字符串，判断括号是否按正确顺序闭合。

## 示例

```txt
输入：s = "()[]{}"
输出：true
```

## 解题思路

- 遇到左括号就压栈，遇到右括号就检查栈顶是否匹配。
- 若中途无法匹配，直接返回 false。
- 遍历结束后栈为空，说明所有括号都成功配对。

## 代码实现（JavaScript）

```javascript
var isValid = function (s) {
  const map = {
    ")": "(",
    "]": "[",
    "}": "{",
  };
  const stack = [];

  for (const ch of s) {
    if (ch === "(" || ch === "[" || ch === "{") {
      stack.push(ch);
    } else {
      if (stack.pop() !== map[ch]) return false;
    }
  }

  return stack.length === 0;
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)
