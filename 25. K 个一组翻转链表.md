# K 个一组翻转链表

## 题目描述

给你一个链表，每 k 个节点一组进行翻转，不足 k 个的部分保持原状。

## 示例

```txt
输入：head = [1,2,3,4,5], k = 2
输出：[2,1,4,3,5]
```

## 解题思路

- 每次先检查从当前起点开始是否还有完整的 k 个节点。
- 若有，就把这一段原地翻转，再接回前后链表。
- 用 dummy + pre 维护每一组翻转前的位置。

## 代码实现（JavaScript）

```javascript
var reverseKGroup = function (head, k) {
  const dummy = new ListNode(0, head);
  let pre = dummy;

  while (true) {
    let tail = pre;
    for (let i = 0; i < k; i++) {
      tail = tail.next;
      if (tail === null) return dummy.next;
    }

    const next = tail.next;
    let prev = next;
    let cur = pre.next;

    while (cur !== next) {
      const temp = cur.next;
      cur.next = prev;
      prev = cur;
      cur = temp;
    }

    const start = pre.next;
    pre.next = tail;
    pre = start;
  }
};
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)
