# 寻找旋转排序数组中的最小值

## 题目描述

给定一个不含重复元素的旋转升序数组，返回其中最小元素。

## 示例

```txt
输入：nums = [3,4,5,1,2]
输出：1
```

## 解题思路

- 最小值一定出现在无序的那一半区间里。
- 若 nums[mid] > nums[right]，说明最小值在右边；否则在左边或就是 mid。
- 最终 left 会停在最小值位置。

## 代码实现（JavaScript）

```javascript
var findMin = function (nums) {
  let left = 0;
  let right = nums.length - 1;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (nums[mid] > nums[right]) {
      left = mid + 1;
    } else {
      right = mid;
    }
  }

  return nums[left];
};
```

## 复杂度分析

- 时间复杂度：O(log n)
- 空间复杂度：O(1)
