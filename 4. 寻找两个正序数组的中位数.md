# 寻找两个正序数组的中位数

## 题目描述

给定两个从小到大排序的数组，请在整体有序视角下求它们的中位数，要求时间复杂度尽量低。

## 示例

```txt
输入：nums1 = [1,3], nums2 = [2]
输出：2.00000
```

## 解题思路

- 对较短数组做二分，寻找一个划分位置，使左右两边元素个数相等。
- 当 leftMax1 <= rightMin2 且 leftMax2 <= rightMin1 时，说明划分合法。
- 奇数长度时答案是左半部分最大值，偶数长度时答案是左右边界的平均值。

## 代码实现（JavaScript）

```javascript
var findMedianSortedArrays = function (nums1, nums2) {
  if (nums1.length > nums2.length) {
    return findMedianSortedArrays(nums2, nums1);
  }

  const m = nums1.length;
  const n = nums2.length;
  let left = 0;
  let right = m;

  while (left <= right) {
    const i = Math.floor((left + right) / 2);
    const j = Math.floor((m + n + 1) / 2) - i;

    const leftMax1 = i === 0 ? -Infinity : nums1[i - 1];
    const rightMin1 = i === m ? Infinity : nums1[i];
    const leftMax2 = j === 0 ? -Infinity : nums2[j - 1];
    const rightMin2 = j === n ? Infinity : nums2[j];

    if (leftMax1 <= rightMin2 && leftMax2 <= rightMin1) {
      if ((m + n) % 2 === 1) {
        return Math.max(leftMax1, leftMax2);
      }
      return (
        (Math.max(leftMax1, leftMax2) + Math.min(rightMin1, rightMin2)) / 2
      );
    }

    if (leftMax1 > rightMin2) {
      right = i - 1;
    } else {
      left = i + 1;
    }
  }

  return 0;
};
```

## 复杂度分析

- 时间复杂度：O(log(min(m, n)))
- 空间复杂度：O(1)
